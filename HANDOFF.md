# Handoff

Current state: empty Phase 1 scaffold. The current experimental source remains
under the parent repository's `ffmpeg-mim-handoff/` directory. Live playback
still has known audio-only and initial-stall failures.

